
  # Create design from text

  This is a code bundle for Create design from text. The original project is available at https://www.figma.com/design/GBnLC818QJ25k5VF6LX04D/Create-design-from-text.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  