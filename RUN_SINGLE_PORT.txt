
Run Everything on ONE PORT

1. Install backend deps
cd backend
pip install fastapi uvicorn python-multipart opencv-python numpy

2. Run server
uvicorn single_port_server:app --reload --port 8000

Open browser:
http://localhost:8000

FastAPI serves:
- Frontend UI
- Backend API

Both run on SAME PORT.
